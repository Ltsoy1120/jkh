import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: "#1EA133"
        },
        secondary: {
            main: "#6B8795"
        }
    },
    typography: {
        fontFamily: "Montserrat"
    }
})
